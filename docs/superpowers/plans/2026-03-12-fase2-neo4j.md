# Movie Recommender — Fase 2: Vector Store com Neo4j — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refatorar a Fase 1 para usar o padrão Strategy no acesso ao vector store, permitindo trocar a implementação de arquivo JSON por Neo4j via variável de ambiente.

**Architecture:** Criar uma interface comum (`save` + `similaritySearch`) com duas implementações — `json.js` (extrai lógica atual) e `neo4j.js` (nova, via LangChain). Uma factory em `src/vectorStore/index.js` seleciona a implementação com base em `VECTOR_STORE`. Os scripts `ingest.js` e `recommend.js` são simplificados para depender apenas da interface.

**Tech Stack:** Node.js 22+, `@langchain/community` (Neo4jVectorStore, HuggingFaceTransformersEmbeddings), `neo4j-driver`, Docker Compose, Node.js built-in test runner

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `src/vectorStore/json.js` | Criar | Implementação JSON: gera embeddings, salva/busca em arquivo |
| `src/vectorStore/neo4j.js` | Criar | Implementação Neo4j: via LangChain Neo4jVectorStore |
| `src/vectorStore/index.js` | Criar | Factory: seleciona implementação via `VECTOR_STORE` env |
| `scripts/ingest.js` | Modificar | Delegar `save()` para o vectorStore; remover lógica de embedding/arquivo |
| `scripts/recommend.js` | Modificar | Delegar `similaritySearch()` para o vectorStore; remover imports diretos |
| `tests/vectorStore/json.test.js` | Criar | Testes unitários para a implementação JSON |
| `tests/recommend.test.js` | Deletar | Lógica migrada para `json.test.js` |
| `docker-compose.yml` | Criar | Sobe Neo4j local para Fase 2 |
| `package.json` | Modificar | Adicionar deps + scripts `infra:up`/`infra:down` |

**Interface do vectorStore (contrato compartilhado):**
```javascript
// embedFn é injetado para permitir testes — usa `embed` do HuggingFace por padrão
vectorStore.save(movies: Array<{ id, title, overview, rating, genres }>): Promise<void>
vectorStore.similaritySearch(query: string, topK?: number): Promise<Array<{ id, title, overview, rating, genres, score }>>
```

---

## Chunk 1: JSON Vector Store

### Task 1: Criar `src/vectorStore/json.js`

**Files:**
- Create: `src/vectorStore/json.js`
- Create: `tests/vectorStore/json.test.js`
- Delete: `tests/recommend.test.js`

- [ ] **Step 1: Criar a estrutura de diretório**

```bash
mkdir -p src/vectorStore tests/vectorStore
```

- [ ] **Step 2: Escrever os testes**

`tests/vectorStore/json.test.js`:
```javascript
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { writeFile, unlink, readFile } from 'node:fs/promises'
import { join } from 'node:path'
import { tmpdir } from 'node:os'
import { createJsonVectorStore } from '../../src/vectorStore/json.js'

// Stub de embed: vetores simples que representam "espaço" vs "romance"
const spaceEmbed = async () => [1, 0, 0]
const romanceEmbed = async () => [0, 0, 1]

function makeTmpPath() {
  return join(tmpdir(), `vs-test-${Date.now()}.json`)
}

test('save persiste os filmes com embeddings no arquivo JSON', async (t) => {
  const path = makeTmpPath()
  t.after(() => unlink(path).catch(() => {}))

  const movies = [
    { id: 1, title: 'Gravity', overview: 'Two astronauts in space', rating: 7.7, genres: 'Sci-Fi' },
  ]

  const store = createJsonVectorStore(path, spaceEmbed)
  await store.save(movies)

  const saved = JSON.parse(await readFile(path, 'utf-8'))
  assert.equal(saved.length, 1)
  assert.equal(saved[0].title, 'Gravity')
  assert.deepEqual(saved[0].embedding, [1, 0, 0])
})

test('similaritySearch retorna os K filmes mais similares ordenados por score', async (t) => {
  const path = makeTmpPath()
  t.after(() => unlink(path).catch(() => {}))

  const movies = [
    { id: 1, title: 'Gravity', overview: 'Astronauts in space', rating: 7.7, genres: 'Sci-Fi', embedding: [1, 0, 0] },
    { id: 2, title: 'The Notebook', overview: 'A love story', rating: 7.9, genres: 'Romance', embedding: [0, 0, 1] },
    { id: 3, title: 'Interstellar', overview: 'Space wormhole', rating: 8.6, genres: 'Sci-Fi', embedding: [0.95, 0.05, 0] },
  ]
  await writeFile(path, JSON.stringify(movies))

  // query embedding próximo de "espaço" → deve retornar Gravity e Interstellar
  const store = createJsonVectorStore(path, spaceEmbed)
  const results = await store.similaritySearch('space adventure', 2)

  assert.equal(results.length, 2)
  // Gravity e Interstellar são mais similares ao vetor [1,0,0]
  const titles = results.map((r) => r.title)
  assert.ok(titles.includes('Gravity'), 'deve incluir Gravity')
  assert.ok(titles.includes('Interstellar'), 'deve incluir Interstellar')
  assert.ok(results[0].score >= results[1].score, 'resultados devem estar ordenados por score')
})

test('similaritySearch resultado contém os campos esperados', async (t) => {
  const path = makeTmpPath()
  t.after(() => unlink(path).catch(() => {}))

  const movies = [
    { id: 42, title: 'Test Movie', overview: 'Test overview', rating: 8.0, genres: 'Drama', embedding: [1, 0] },
  ]
  await writeFile(path, JSON.stringify(movies))

  const store = createJsonVectorStore(path, async () => [1, 0])
  const results = await store.similaritySearch('test query', 1)

  assert.ok('id' in results[0], 'deve ter id')
  assert.ok('title' in results[0], 'deve ter title')
  assert.ok('overview' in results[0], 'deve ter overview')
  assert.ok('rating' in results[0], 'deve ter rating')
  assert.ok('genres' in results[0], 'deve ter genres')
  assert.ok('score' in results[0], 'deve ter score')
})

test('similaritySearch score está entre 0 e 1', async (t) => {
  const path = makeTmpPath()
  t.after(() => unlink(path).catch(() => {}))

  const movies = [
    { id: 1, title: 'Movie', overview: 'Overview', rating: 7.0, genres: 'Action', embedding: [0.5, 0.5, 0] },
  ]
  await writeFile(path, JSON.stringify(movies))

  const store = createJsonVectorStore(path, async () => [1, 0, 0])
  const results = await store.similaritySearch('query', 1)

  assert.ok(results[0].score >= 0 && results[0].score <= 1, `score fora do range: ${results[0].score}`)
})
```

- [ ] **Step 3: Rodar os testes e verificar que falham**

```bash
node --test tests/vectorStore/json.test.js
```

Expected: erro `Cannot find module '../../src/vectorStore/json.js'`

- [ ] **Step 4: Implementar `src/vectorStore/json.js`**

```javascript
import { readFile, writeFile } from 'node:fs/promises'
import { embed as defaultEmbed } from '../embeddings.js'
import { cosineSimilarity } from '../similarity.js'
import { combinedScore } from '../scoring.js'

const DEFAULT_PATH = new URL('../../data/vector-store.json', import.meta.url).pathname

/**
 * Cria um vector store baseado em arquivo JSON.
 *
 * @param {string}   storePath - Caminho do arquivo JSON (padrão: data/vector-store.json)
 * @param {Function} embedFn   - Função de embedding (padrão: embed do HuggingFace). Injetável para testes.
 */
export function createJsonVectorStore(storePath = DEFAULT_PATH, embedFn = defaultEmbed) {
  return {
    /**
     * Gera embeddings para cada filme e persiste em JSON.
     * @param {Array<{ id, title, overview, rating, genres }>} movies
     */
    async save(movies) {
      const indexed = []
      for (const [i, movie] of movies.entries()) {
        process.stdout.write(`\r⚙ Gerando embeddings: ${i + 1}/${movies.length}`)
        const embedding = await embedFn(movie.overview)
        indexed.push({ ...movie, embedding })
      }
      console.log()
      await writeFile(storePath, JSON.stringify(indexed, null, 2))
      console.log(`💾 Salvo em ${storePath}`)
    },

    /**
     * Busca os K filmes mais similares usando score combinado.
     * @param {string} query  - Texto de busca
     * @param {number} topK   - Número de resultados (padrão: 5)
     * @returns {Promise<Array<{ id, title, overview, rating, genres, score }>>}
     */
    async similaritySearch(query, topK = 5) {
      const queryEmbedding = await embedFn(query)
      const movies = JSON.parse(await readFile(storePath, 'utf-8'))

      return movies
        .map((movie) => ({
          id: movie.id,
          title: movie.title,
          overview: movie.overview,
          rating: movie.rating,
          genres: movie.genres,
          score: combinedScore(
            cosineSimilarity(queryEmbedding, movie.embedding),
            movie.rating,
            movie.genres,
            query
          ),
        }))
        .sort((a, b) => b.score - a.score)
        .slice(0, topK)
    },
  }
}
```

- [ ] **Step 5: Rodar os testes e verificar que passam**

```bash
node --test tests/vectorStore/json.test.js
```

Expected:
```
✔ save persiste os filmes com embeddings no arquivo JSON
✔ similaritySearch retorna os K filmes mais similares ordenados por score
✔ similaritySearch resultado contém os campos esperados
✔ similaritySearch score está entre 0 e 1
ℹ tests 4
ℹ pass 4
```

- [ ] **Step 6: Deletar `tests/recommend.test.js`** (lógica migrada para `json.test.js`)

```bash
rm tests/recommend.test.js
```

- [ ] **Step 7: Verificar que o restante dos testes ainda passa**

```bash
npm test
```

Expected: testes de `similarity.test.js` e `ingest.test.js` passando (recommend.test.js removido).

- [ ] **Step 8: Commit**

```bash
git add src/vectorStore/json.js tests/vectorStore/json.test.js tests/recommend.test.js
git commit -m "feat: add JSON vector store implementation with strategy interface"
```

---

## Chunk 2: Factory + Refatoração dos Scripts

> **Pré-condição:** Chunk 1 concluído. `src/vectorStore/json.js` existe e `tests/recommend.test.js` foi deletado.
> `createJsonVectorStore().save()` gera os embeddings internamente chamando `embedFn(movie.overview)` — portanto `ingest.js` não precisa mais chamar `embed()` diretamente.

### Task 2: Criar `src/vectorStore/index.js`

**Files:**
- Create: `src/vectorStore/index.js`

- [ ] **Step 0: Verificar pré-condições do Chunk 1 e deletar `tests/recommend.test.js`**

```bash
# Verificar que o json.js do Chunk 1 existe
node --input-type=module -e "import('./src/vectorStore/json.js').then(() => console.log('✅ json.js ok'))"

# Deletar recommend.test.js (pode ter ficado para trás)
ls tests/recommend.test.js 2>/dev/null && git rm tests/recommend.test.js && git commit -m "test: remove recommend.test.js (migrado para vectorStore/json.test.js)" || echo "Já deletado"
```

- [ ] **Step 1: Implementar a factory**

`src/vectorStore/index.js`:
```javascript
import { createJsonVectorStore } from './json.js'

/**
 * Retorna a implementação de vector store conforme a variável de ambiente VECTOR_STORE.
 * Valores: 'json' (padrão) | 'neo4j'
 *
 * É async para permitir dynamic import do neo4j.js (que tem dependências pesadas)
 * sem quebrar o carregamento do módulo quando VECTOR_STORE=json.
 */
export async function getVectorStore() {
  const type = process.env.VECTOR_STORE || 'json'

  if (type === 'neo4j') {
    const { createNeo4jVectorStore } = await import('./neo4j.js')
    return createNeo4jVectorStore()
  }

  return createJsonVectorStore()
}
```

> **Nota:** `neo4j.js` usa dynamic import para que seu carregamento (e as dependências pesadas como `@langchain/community`) só aconteça quando `VECTOR_STORE=neo4j`. Isso evita erro de módulo não encontrado enquanto `neo4j.js` ainda não existe.

- [ ] **Step 2: Commit**

```bash
git add src/vectorStore/index.js
git commit -m "feat: add vector store factory with strategy pattern"
```

---

### Task 3: Refatorar `scripts/ingest.js`

**Files:**
- Modify: `scripts/ingest.js`

O `ingest.js` deve:
- Continuar exportando `parseCSV`, `normalizeMovie`, `filterMovies` (os testes existentes dependem disso)
- Substituir a lógica de embedding + `writeFile` pela chamada `vectorStore.save(valid)`

- [ ] **Step 1: Verificar que os testes de ingest passam antes da mudança**

```bash
node --test tests/ingest.test.js
```

Expected: 4 testes passando.

- [ ] **Step 2: Atualizar `scripts/ingest.js`**

```javascript
import { createReadStream } from 'node:fs'
import { parse } from 'csv-parse'
import { getVectorStore } from '../src/vectorStore/index.js'

const CSV_PATH = new URL('../data/imdb_top_1000.csv', import.meta.url).pathname

export function parseCSV(filePath) {
  return new Promise((resolve, reject) => {
    const records = []
    createReadStream(filePath)
      .pipe(parse({ columns: true, skip_empty_lines: true }))
      .on('data', (row) => records.push(row))
      .on('end', () => resolve(records))
      .on('error', reject)
  })
}

/**
 * Normaliza uma linha do CSV do IMDB para o formato interno.
 */
export function normalizeMovie(row, index) {
  return {
    id: index + 1,
    title: row.Series_Title,
    overview: row.Overview,
    rating: parseFloat(row.IMDB_Rating) || 0,
    genres: row.Genre || '',
  }
}

export function filterMovies(movies) {
  const valid = []
  let skipped = 0

  for (const movie of movies) {
    if (!movie.overview?.trim()) {
      console.warn(`⚠ Ignorando "${movie.title}" — overview vazio`)
      skipped++
    } else {
      valid.push(movie)
    }
  }

  return { valid, skipped }
}

async function main() {
  console.log('📖 Lendo CSV...')
  const rows = await parseCSV(CSV_PATH)
  const normalized = rows.map(normalizeMovie)
  const { valid, skipped } = filterMovies(normalized)
  console.log(`📊 ${valid.length} filmes para indexar, ${skipped} ignorados\n`)

  const vectorStore = await getVectorStore()
  await vectorStore.save(valid)
  console.log(`✅ ${valid.length} filmes indexados`)
}

if (process.argv[1] === new URL(import.meta.url).pathname) {
  main().catch(console.error)
}
```

- [ ] **Step 3: Verificar que os testes de ingest ainda passam**

```bash
node --test tests/ingest.test.js
```

Expected: 4 testes passando (as funções exportadas não mudaram).

- [ ] **Step 4: Commit**

```bash
git add scripts/ingest.js
git commit -m "refactor: ingest delegates to vectorStore.save()"
```

---

### Task 4: Refatorar `scripts/recommend.js`

**Files:**
- Modify: `scripts/recommend.js`

O `recommend.js` deve:
- Remover imports diretos de `embed`, `cosineSimilarity`, `combinedScore`
- Usar `vectorStore.similaritySearch(query, topK)` via factory

- [ ] **Step 1: Atualizar `scripts/recommend.js`**

```javascript
import { getVectorStore } from '../src/vectorStore/index.js'

if (process.argv[1] === new URL(import.meta.url).pathname) {
  const query = process.argv[2]
  if (!query) {
    console.error('Uso: node scripts/recommend.js "Título do Filme"')
    process.exit(1)
  }

  console.log(`\n🔍 Buscando filmes similares a "${query}"...\n`)

  const vectorStore = await getVectorStore()
  const results = await vectorStore.similaritySearch(query)

  results.forEach((movie, i) => {
    const preview = movie.overview.slice(0, 50) + '...'
    console.log(
      `${i + 1}. ${movie.title.padEnd(28)} (score: ${movie.score.toFixed(2)}) ⭐ ${movie.rating.toFixed(1)} | ${movie.genres} — ${preview}`
    )
  })
}
```

- [ ] **Step 2: Rodar todos os testes**

```bash
npm test
```

Expected: 19 testes passando (similarity: 5, scoring: 6, ingest: 4, vectorStore/json: 4).

- [ ] **Step 3: Commit**

```bash
git add scripts/recommend.js
git commit -m "refactor: recommend delegates to vectorStore.similaritySearch()"
```

---

## Chunk 3: Neo4j + Infraestrutura

### Task 5: Docker Compose + Dependências

**Files:**
- Create: `docker-compose.yml`
- Modify: `package.json`

- [ ] **Step 1: Criar `docker-compose.yml`**

```yaml
services:
  neo4j:
    image: neo4j:5
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
```

- [ ] **Step 2: Instalar dependências do LangChain**

```bash
npm install @langchain/community @langchain/core neo4j-driver
```

- [ ] **Step 3: Adicionar scripts de infraestrutura ao `package.json`**

Adicionar dentro de `"scripts"`:
```json
"infra:up": "docker compose up -d",
"infra:down": "docker compose down"
```

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml package.json package-lock.json
git commit -m "feat: add Neo4j docker-compose and LangChain dependencies"
```

---

### Task 6: Criar `src/vectorStore/neo4j.js`

**Files:**
- Create: `src/vectorStore/neo4j.js`

> **Nota:** Esta implementação não tem testes unitários — a integração com Neo4j requer a infra rodando. A validação é feita via E2E na Task 7.

- [ ] **Step 1: Implementar `src/vectorStore/neo4j.js`**

```javascript
import { Neo4jVectorStore } from '@langchain/community/vectorstores/neo4j_vector'
import { HuggingFaceTransformersEmbeddings } from '@langchain/community/embeddings/huggingface_transformers'
import { Document } from '@langchain/core/documents'

/**
 * Cria um vector store baseado em Neo4j via LangChain.
 * A instância de embeddings é criada uma única vez e reutilizada.
 */
export function createNeo4jVectorStore() {
  const embeddings = new HuggingFaceTransformersEmbeddings({ model: 'Xenova/all-MiniLM-L6-v2' })
  const config = {
    url: process.env.NEO4J_URI || 'bolt://localhost:7687',
    username: process.env.NEO4J_USER || 'neo4j',
    password: process.env.NEO4J_PASSWORD || 'password',
    indexName: 'movie_embeddings',
    nodeLabel: 'Movie',
    textNodeProperty: 'overview',
    embeddingNodeProperty: 'embedding',
  }

  return {
    /**
     * Converte filmes em Documents do LangChain e indexa no Neo4j.
     * @param {Array<{ id, title, overview, rating, genres }>} movies
     */
    async save(movies) {
      const docs = movies.map(
        (movie) =>
          new Document({
            pageContent: movie.overview,
            metadata: {
              id: movie.id,
              title: movie.title,
              overview: movie.overview,
              rating: movie.rating || 0,
              genres: movie.genres || '',
            },
          })
      )

      console.log(`⏳ Indexando ${docs.length} filmes no Neo4j...`)
      const store = await Neo4jVectorStore.fromDocuments(docs, embeddings, config)
      await store.close()
      console.log(`✅ ${docs.length} filmes indexados no Neo4j`)
    },

    /**
     * Busca os K filmes mais similares no Neo4j.
     * @param {string} query  - Texto de busca
     * @param {number} topK   - Número de resultados (padrão: 5)
     * @returns {Promise<Array<{ id, title, overview, rating, genres, score }>>}
     */
    async similaritySearch(query, topK = 5) {
      const store = await Neo4jVectorStore.fromExistingIndex(embeddings, config)
      const results = await store.similaritySearchWithScore(query, topK)
      await store.close()

      return results.map(([doc, score]) => ({
        id: doc.metadata.id,
        title: doc.metadata.title,
        overview: doc.metadata.overview,
        rating: doc.metadata.rating,
        genres: doc.metadata.genres,
        score,
      }))
    },
  }
}
```

- [ ] **Step 2: Rodar os testes para garantir que nada quebrou**

```bash
npm test
```

Expected: todos os testes passando.

- [ ] **Step 3: Commit**

```bash
git add src/vectorStore/neo4j.js
git commit -m "feat: add Neo4j vector store implementation via LangChain"
```

---

### Task 7: Verificação E2E com Neo4j

> **Pré-requisito:** Docker rodando. Arquivo `data/imdb_top_1000.csv` presente.

- [ ] **Step 1: Subir o Neo4j**

```bash
npm run infra:up
```

Expected: container `neo4j` rodando. Aguarde ~10 segundos para o Neo4j inicializar.

- [ ] **Step 2: Verificar que o Neo4j está acessível**

```bash
curl -s http://localhost:7474 | head -5
```

Expected: resposta JSON com informações do Neo4j.

- [ ] **Step 3: Rodar o ingest com Neo4j**

```bash
VECTOR_STORE=neo4j node scripts/ingest.js
```

Expected:
```
📖 Lendo CSV...
📊 1000 filmes para indexar, 0 ignorados

⏳ Indexando 1000 filmes no Neo4j...
✅ 1000 filmes indexados no Neo4j
✅ 1000 filmes indexados
```

- [ ] **Step 4: Testar uma recomendação com Neo4j**

```bash
VECTOR_STORE=neo4j node scripts/recommend.js "Interstellar"
```

Expected: top 5 filmes de sci-fi/espaço listados.

- [ ] **Step 5: Testar com a implementação JSON para garantir que ainda funciona**

> **Pré-requisito:** O `data/vector-store.json` precisa existir. Se não existir, rode primeiro `node scripts/ingest.js` (sem `VECTOR_STORE=neo4j`).

```bash
node scripts/recommend.js "The Godfather"
```

Expected: filmes de drama/crime listados (usa `VECTOR_STORE=json` por padrão).

- [ ] **Step 6: Derrubar a infra**

```bash
npm run infra:down
```

- [ ] **Step 7: Rodar os testes finais**

```bash
npm test
```

Expected: todos os testes passando.

- [ ] **Step 8: Commit final**

```bash
git add .
git commit -m "feat: complete phase 2 - Neo4j vector store with strategy pattern"
git push origin main
```
